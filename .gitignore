# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# OS / editor / local tooling
.DS_Store
.vscode/
.idea/

# Generated data under assets/. `assets/plots/*.png` is the ONLY tracked asset --
# everything else is reproducible by rerunning the `main.py` stages.
#
# Whole training tree: checkpoints, metric logs, intermediate models, TensorBoard.
assets/learned_models/

# Result data written alongside the figures.
assets/*.json
assets/plots/*.json
assets/plots/*.csv

# Safety net in case a checkpoint or event file is written outside the tree above.
assets/**/*.p
events.out.tfevents.*
